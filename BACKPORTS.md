# Alpha bugfix synchronization

Behavioral fixes found while preparing the alpha are applied to the source
QSPKit checkout with the same regression test. Release-only cleanup is not
backported.

| Date | Fix | Alpha paths | Source status | Verification |
| --- | --- | --- | --- | --- |
| 2026-08-27 | SimKit phase cache distinguishes relative and absolute start times and verifies cached input snapshots | `SimKit/src/cache.jl`, `SimKit/src/pipeline.jl`, `SimKit/test/runtests.jl`, `SimKit/docs/src/caching.md` | Mirrored to the original checkout; awaiting a source-repository commit | Current alpha SimKit 203/203; source SimKit 202/202 |
| 2026-08-27 | ConfigKit narrows the ModelingToolkit/DynamicQuantities diagnostic workaround to the exact broken `safe_get_unit(::Any, ::Any)` method and preserves specialized overloads | `ConfigKit/src/ConfigKit.jl`, `ConfigKit/test/runtests.jl`, `ConfigKit/test/mtk_dimension_error_patch_test.jl` | Mirrored to the original checkout; awaiting a source-repository commit | ConfigKit 790/790 in both checkouts |
| 2026-08-28 | ConfigKit honors strict keyfile parsing, forwards `populate!` strictness to updates, and routes path-based `solve_for` to population | `ConfigKit/src/keyfile/parser.jl`, `ConfigKit/src/keyfile/populate.jl`, `ConfigKit/test/keyfile/parser_edge_cases_test.jl`, `ConfigKit/test/update/populate_test.jl`, `ConfigKit/docs/src/api_reference.md`, `ConfigKit/docs/src/keyfile_format.md`, `ConfigKit/docs/src/variants.md` | Mirrored to the original checkout; awaiting a source-repository commit | ConfigKit 805/805 in both checkouts; both manuals built |
| 2026-08-28 | CondaR detects an absent or mismatched generated RCall configuration and rebuilds RCall against its managed R before the first import | `CondaR/src/CondaR.jl`, `CondaR/test/runtests.jl` | Mirrored with focused tests to the original checkout; awaiting a source-repository commit | Alpha CondaR 16/16; source regression 4/4; alpha manual built; 4-entrypoint matrix validated |
| 2026-08-28 | ShowKit's provenance assertions capture the caller before entering the `@test` macro so `Pkg.test` does not substitute `Test.jl` for the fixture script | `ShowKit/test/runtests.jl` | Mirrored to the original checkout; awaiting a source-repository commit | Fix-focused verification before the alpha wrapper-contract expansion: alpha 113/113; source 110/110 |
| 2026-08-28 | QSPReports preserves derived expressions in plain tables, widens numeric columns before LaTeX assignment, handles iterators of pairs, validates metadata policies, and gives accessor-based variant comparisons the correct parameter-only default | `QSPReports/src/parameter_tables.jl`, `QSPReports/test/runtests.jl` | Mirrored to the original checkout; awaiting a source-repository commit | Alpha QSPReports 133/133; source QSPReports 60/60; alpha manual built; 23-entrypoint matrix validated |
| 2026-08-28 | TargKit rejects invalid ordinary predictions, observations, series elements, unresolved convention sources, and missing required bounds with contextual errors while reserving synthetic penalties for explicit simulation failure and opt-in bounds handling | `TargKit/src/scoring.jl`, `TargKit/src/objective.jl`, `TargKit/src/types.jl`, `TargKit/test/runtests.jl`, `TargKit/docs/` | Mirrored to the original checkout; awaiting a source-repository commit | TargKit 262/262 in both checkouts; alpha manual built |
| 2026-08-28 | StoreKit preserves keyword-only `Base.open` calls while recording their effective read/write mode, rather than combining a default mode string with incompatible I/O keywords | `StoreKit/src/file_tracking.jl`, `StoreKit/test/tracking_test.jl`, `StoreKit/docs/src/file_tracking.md` | Mirrored to the original checkout; awaiting a source-repository commit | Alpha StoreKit 164/164; source StoreKit 142/142; alpha manual built |
| 2026-08-28 | ShowKit and SpecKit make optional R dependency discovery read-only: ordinary use no longer installs GitHub/CRAN/TeX packages or edits an installed package namespace, and direct Conda dependencies use reviewed exact versions | `ShowKit/src/r_backend.jl`, `ShowKit/CondaPkg.toml`, `ShowKit/docs/src/getting_started.md`, `SpecKit/src/backend.jl`, `SpecKit/README.md`, `CondaR/CondaPkg.toml` | Mirrored to the original checkout; awaiting a source-repository commit | Alpha ShowKit 2289/2289 and SpecKit 68/68; source ShowKit 110/110 and SpecKit 46/46; fixed CondaR boundary alpha 16/16 and source regression 4/4 |

Add one row for each later behavioral fix. Keep commit identifiers and other
source-repository coordinates in the maintainers' private release record.
